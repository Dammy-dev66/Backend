{
  "name": "finbar-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless backend for Fin's custom booking flow.",
  "scripts": {
    "test": "node --test",
    "dev": "node scripts/local-dev.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^2.8.0",
    "stripe": "^22.5.0"
  }
}
